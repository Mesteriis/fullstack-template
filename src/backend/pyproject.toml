[project]
name = "registries-backend"
version = "0.1.0"
description = "Backend service for the registries monorepo"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.135.2",
    "uvicorn>=0.42.0",
]
